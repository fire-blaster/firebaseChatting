import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    safeContainer: {
        flex: 1,
        backgroundColor: "black"
    },
    container: {
        margin: 50
    },
    userInput: {
        borderColor: "gray",
        borderWidth: 0.5,
        borderRadius: 10
    },
    emailInput: {
        borderColor: "gray",
        borderWidth: 0.5,
        borderRadius: 10,
        marginTop: 5
    },
    passwordInput: {
        borderColor: "gray",
        borderWidth: 0.5,
        borderRadius: 10,
        marginTop: 5
    },
})